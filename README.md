# ALLNI ACTIVE Website (Static Export)

## Run locally
```bash
npm install
npm run dev
```

## Build static files
```bash
npm install
npm run build
```

After build, static files will be generated in the `out` folder.

## Deploy options
- Cloudflare Pages
- Any static hosting
- Vercel
